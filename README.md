# CODE FOR GOOD 2018 -- AUTICTICA

You need to install ``nodejs`` and ``firebase-tools``


To build, do the following:
- ```cd autistica```
- ```npm install```
- ```npm run build && npm run deploy```
